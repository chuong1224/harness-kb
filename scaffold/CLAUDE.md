---
gate_ignore: true
---

# Claude entrypoint

Read and follow `AGENTS.md` at the vault root before acting. `AGENTS.md` is the single human-owned
agent contract; this file deliberately stays thin so the two entrypoints cannot drift apart.
