# Claude entrypoint for this repository

Read and follow `AGENTS.md` at the repository root before editing. `AGENTS.md` is the single
repo-local contract; this file stays thin so agent-specific entrypoints cannot drift apart.
